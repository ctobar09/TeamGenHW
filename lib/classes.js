const { questions } = require('./app');

console.log(questions)

class Employee 
{
    constructor(name, id, role, title) {
    this.name = name;
    this.id = id;
    this.title = title;
    this.role = role;
    this.getName() = this.name;
    this.getTitle() = this.title;
    this.getID() = this.id;
    this.getEmail() = this.email;
    this.getRole() = this.role;
}
    }


 


const Manager = new Employee(name, id, title, role)